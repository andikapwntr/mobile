import React,{Component} from 'react';
import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    StatusBar,
  } from 'react-native';


  class Header extends Component {
      state = {  }
      render() { 
          return (  );
      }
  }
   
  export default Header;